name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.2'
      SERVERPOD_PASSWORD_database: 'PfsZzmuzNH2sdPtIy8vkcgmDsZ6BJjHi'
      SERVERPOD_PASSWORD_redis: 'GoBr29P6JQDIaC1VeqAZmLD9lfTJ7W4g'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: sinar_apps_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: sinar_apps_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: sinar_apps_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: sinar_apps_server
        run: serverpod generate

      - name: Run tests
        working-directory: sinar_apps_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: sinar_apps_server
        run: docker compose down -v
